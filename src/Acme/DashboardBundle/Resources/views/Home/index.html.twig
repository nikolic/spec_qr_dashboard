{% extends 'AcmeDashboardBundle::layout.html.twig' %}
{% block body %}


        <section id="0" class="window">
            <div class="content">
                    <h1>Loyalty</h1>
                    <h2>Get your qrcodes right now</h2>
            </div>
        </section>

        <section id="1" class="window">
            <div class="content">
                <h2>
                Loyalty<br/>
                | -- QRcode<br/>
                | -- | -- <span class="highlight"><a class="content_nav" href="#create">Create</a></span><br/>
                | -- Statistics & Reports<br/>
                | -- | -- <span class="highlight"><a class="content_nav" href="#statistics">Statistics</a></span><br/>
                | -- | -- <span class="highlight">
                				<a class="small button content_nav" href="{{ path('acme_dashboard_reports', { 'name': app.user.username }) }}">Reports</a></span><br/>
                | -- Settings<br/>
                | -- | -- <span class="highlight"><a class="content_nav" href="#settings">Weights</a></span><br/>
                | -- | -- <span class="highlight"><a  class="content_nav" href="#settings">QRcodes</a></span><br/>
                | -- Info <br/>
                | -- | -- <span class="highlight"><a class="content_nav" href="#info">How it works</a></span>
                </h2>
            </div>
        </section>

        <section id="2" class="window">
        	<a name="create"></a>
            <div class="content" id="code_generator">

            	<img src="{{asset('images/loader.GIF')}}" />

            </div>
        </section>

        <section id="3" class="window">
        	<a name="statistics"></a>
            <div class="content" id="statistics_and_reports">
            	                
            	<img src="{{asset('images/loader.GIF')}}" />

            </div>
        </section>

        <section id="4" class="window">
        	<a name="settings"></a>
            <div class="content">
                <h1>api</h1>
                <pre>
                    // returns ratio 0-1
                    $('.window:eq(0)').ratioVisible();

                    // returns boolean of whether or not is visible on screen
                    $('.window:eq(0)').isOnScreen();

                    // get most visible window
                    $('.window').getCurrentWindow();


                </pre>
            </div>

        </section>

        <section id="5" class="window">
        	<a name="info"></a>
            <div class="content">
                <a href="https://github.com/nick-jonas/nick-jonas.github.com/tree/master/windows" title="Download Me!" id="btn-wrap">
                    <span class="title">Download Me</span>
                    <div id="info">
                        <p>
                            <strong>Version 0.0.1 /</strong>
                            <span>1 MB</span>
                        </p>
                    </div>
                </a>

                <iframe src="http://ghbtns.com/github-btn.html?user=nick-jonas&repo=windows&type=fork&count=true&size=large" allowtransparency="true" frameborder="0" scrolling="0" width="156px" height="30px" style="margin:50px 0 0 50px;"></iframe>

            </div>
        </section>

		<div id='fixed-bar'>
		  <div id='bar-inner'>
		    <a class='go-top' href='#page-wrapper' title='back to top'>Back to Top</a>
		    <div class="logout_wrap">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <div class="username_bar">Hi {{ app.user.username }}</div>
                    <a class="logout_link" href="{{ path('fos_user_security_logout') }}">{{ 'logout'|trans({}, 'FOSUserBundle') }}</a>
                {% endif %}  
		    </div>
		  </div>
		</div>

        <script>

            $(document).ready(function(){

                var $windows = $('.window');

                $windows.windows({
                    snapping: true,
                    snapSpeed: 500,
                    snapInterval: 1100,
                    onScroll: function(s){},
                    onSnapComplete: function($el){},
                    onWindowEnter: function($el){}
                });

                $(function () {
				  $("#fixed-bar")
				    .css({position:'fixed',bottom:'0px'})
				    .hide();
				  $(window).scroll(function () {
				    if ($(this).scrollTop() > 400) {
				      $('#fixed-bar').fadeIn(200);
				    } else {
				      $('#fixed-bar').fadeOut(200);
				    }
				  });
				  $('.go-top').click(function () {
				    $('html,body').animate({
				      scrollTop: 0
				    }, 1000);
				    return false;
				  });
				});

            });
        </script>


{% endblock %}